# Architecture Decisions: {{PROJECT_NAME}}

**Status:** Decided (v1), pending scaffolding
<!-- Brownfield: change the Status line above to something like "Decided
     (v1), already implemented" -- this doc is documenting an existing,
     working stack, not proposing one that's about to be built. -->
**Companion doc:** {{PRD_PATH}} (product requirements — this doc is implementation)

---

## 1. Stack (v1)

<!-- One row per layer actually needed by this project -- delete rows
     that don't apply (e.g. no "Frontend" row for a CLI tool or library).
     The "Why" column must give a REASON, not just restate the choice --
     tie it back to a stated constraint (existing convention on this
     machine, a hard requirement from the PRD/user, avoiding a second
     toolchain, etc.). If you can't justify a row, that's a sign the
     choice needs more thought before committing it to a document a
     future agent will treat as ground truth.

     Brownfield: every row's "Why" should point at the actual evidence
     found on disk (e.g. "package.json + tsconfig.json already configure
     this; it's the established convention, not a fresh choice"), not a
     hypothetical rationale for a decision that was never actually made
     fresh in this pass. Don't propose alternatives to an already-working
     stack as part of a bootstrap pass. -->

| Layer | Choice | Why |
|---|---|---|
| Language/runtime | {{LANGUAGE}} | {{WHY_LANGUAGE}} |
| Web/API (if any) | {{WEB_FRAMEWORK}} | {{WHY_WEB_FRAMEWORK}} |
| Frontend (if any) | {{FRONTEND}} | {{WHY_FRONTEND}} |
| Local datastore | {{DATASTORE}} | {{WHY_DATASTORE}} |
| Test framework | {{TEST_FRAMEWORK}} | Required by the harness's TDD constraint — see docs/quality-standards.md |
| Packaging/deployment | {{PACKAGING}} | {{WHY_PACKAGING}} |

## 2. Deployment

{{DEPLOYMENT_NOTES}}
<!-- Where does this actually run -- self-hosted (which host/module?),
     a PaaS, a serverless target, or "not deployed, library/CLI only"?
     If self-hosting onto existing infra (e.g. a homelab-iac-style repo),
     name the target module/convention explicitly and link it. -->

## 3. Config as source of truth

<!-- This section is REQUIRED whenever the project has both static
     tuning and any mutable/user-generated state -- which is almost
     always. Two stores, never one file trying to be both: -->

- **{{CONFIG_FILE}}** — every static tunable ({{EXAMPLE_TUNABLES}}).
  Secret fields ship blank, overridden by env vars at deploy.
- **{{STATE_STORE}}** — mutable runtime/user-generated state
  ({{EXAMPLE_STATE}}). This is generated by the running application, not
  tuning, so it never lives in {{CONFIG_FILE}}.

## 4. Frontend/API boundary rule

<!-- Include this section ONLY if the project has both a UI and an API
     (or could plausibly need one later). Delete it entirely for
     API-only / CLI-only / library projects. -->

**Decided:** {{BOUNDARY_RULE_STATEMENT}}
<!-- Standard framing, adapt specifics: "No business logic may live only
     in a template/UI-only route. It lives in the API layer
     ({{API_PREFIX}}), and the UI is just a consumer of it." State WHY --
     usually: keeps a future UI rewrite (different framework, public
     launch, mobile client) cheap because it's "new client against a
     tested API," not a backend rewrite. -->

## 5. {{CORE_DIFFERENTIATOR_NAME}}
<!-- This is numbered 5 assuming §4 (Frontend/API boundary rule) stays.
     If you deleted §4 above (no separate frontend/UI), renumber this
     heading to "## 4." -- don't leave a gap. Also fix any cross-
     references to "§5" elsewhere in this doc (e.g. in DECISIONS.md
     entries written later) to match. -->


<!-- Every non-trivial project has ONE piece of business logic that is
     the actual reason the project exists -- the part that isn't
     boilerplate CRUD. Give it its own numbered section, spelled out
     UNAMBIGUOUSLY, especially any rule that's easy to get subtly wrong
     (e.g. "either signal stale" vs "both signals stale" -- the kind of
     distinction that looks like a rounding error but flips the
     product's entire behavior). If in doubt about the exact rule,
     don't guess -- ask the user via AskUserQuestion before writing this
     section, and flag it in DECISIONS.md as confirmed-with-user. -->

{{CORE_DIFFERENTIATOR_DETAIL}}
